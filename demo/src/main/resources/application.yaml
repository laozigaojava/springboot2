mybatis:
  type-aliases-package: com.newer.demo.bean
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/mybatistest?useSSL=false&serverTimezone=UTC
    username: root
    password: root
  session:
    store-type: redis
  redis:
    host: 127.0.0.1
    port: 6379